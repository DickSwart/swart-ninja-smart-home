---
##############################################################################################
# RESTful
# The rest sensor platform is consuming a given endpoint which is exposed by a RESTful
# API of a device, an application, or a web service.
#
# URL: https://www.home-assistant.io/integrations/rest/
#
# HA Dockermon: https://github.com/philhawthorne/ha-dockermon
# EntityID: sensor.ha_dockermon_nodered
##############################################################################################
platform: rest
resource: http://192.168.1.213:8126/container/nodered
name: HA Dockermon NodeRed
value_template: "{{ value_json.status }}"
json_attributes:
  - state
