---
##################################################################################################################
# Input Boolean
# The input_boolean integration allows the user to define boolean values that can be controlled via
# the frontend and can be used within conditions of automation. This can for example be used to disable
# or enable certain automations.
#
# The preferred way to configure input booleans is via the user interface at Configuration -> Helpers.
# Click the add button and then choose the “Toggle” option.
#
# URL: https://www.home-assistant.io/integrations/input_boolean/
##################################################################################################################
input_boolean: ~
