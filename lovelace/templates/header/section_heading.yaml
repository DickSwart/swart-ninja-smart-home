# lovelace_gen
type: markdown
content: |
  ## {{ title }}
style: |
  ha-card {
    box-shadow: none;
    background: none;
  }
  ha-card > ha-markdown {
    padding: 0 1em;
  }
  ha-icon {
    float: right;
  }
  h2 {
    font-size: 1.6em;
    font-weight: 300;
    padding-left: 10px;
    border-left: 2px solid var(--primary-color);
    font-family: var(--paper-font-title_-_font-family);
  }
